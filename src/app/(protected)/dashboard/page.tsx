

export default function Page(){
    // Server action to onboard the user->if it is a new user
    
    // redirect the user to /dashboard/{id} if statys is 200 | 201. 200->already a user, 201->new user

    // redirect the user to /auth/sign-in if status is 401 | 403->something went wrong

    return (
        <div>Dashboard Page</div>
    )
}